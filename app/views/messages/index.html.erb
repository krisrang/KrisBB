<ul class="unstyled messages">
  <% @messages.includes(:user).reverse.each do |message| %>
  <li class="user<%= message.user.colour %>">
    <div class="username">
      <img src="<%= message.user.avatar.thumb.url %>" />
      <%= message.user.username %>
    </div>
    <div class="message"><%= raw message.html %></div>
    <div class="time">
      <time class="timeago" datetime="<%= message.created_at %>"><%= message.created_at.to_s :long_ordinal %></time>
    </div>
  </li>
  <% end %>
</ul>

<div id="send-message">
  <div id="messagebox">
    <textarea></textarea>
  </div>
  <button id="push-message" class="btn btn-primary"><i class="icon-comment icon-white"></i></button>
</div>

<div id="sidebar">
  <h3 class="usercount">Users</h3>
  <ul class="users unstyled"></ul>
</div>